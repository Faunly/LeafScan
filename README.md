# LeafScan 🌿

Интеллектуальная система для детекции болезней растений по фотографиям с последующей генерацией рекомендаций по лечению с помощью ИИ.

## 📖 Описание проекта

Приложение позволяет пользователю загрузить фотографию растения, после чего:

1. **Компьютерное зрение (YOLOv11)** определяет наличие заболевания и локализует его на снимке.
2. **LLM (GigaChat)** генерирует 3 конкретных совета по уходу и лечению, если болезнь обнаружена.
3. **Веб-интерфейс** отображает результат с отрисованными рамками детекции и текстовыми рекомендациями.

---

## 🛠 Технологический стек

* **Backend:** FastAPI (Python)
* **ML Model:** YOLOv11 (Ultralytics)
* **AI Integration:** GigaChat API
* **Frontend:** HTML5, CSS3, JavaScript (Axios)

---

## 🚀 Быстрый запуск

### 1. Подготовка окружения

Убедитесь, что у вас установлен Python 3.9+. Клонируйте репозиторий:

```bash
git clone https://github.com/Faunly/LeafScan.git
cd LeafScan

```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt

```

### 3. Настройка API и безопасности

Создайте файл `.env` в корневой папке и добавьте туда ваш ключ GigaChat:

```env
GIGACHAT_AUTH_KEY=ваш_авторизационный_ключ

```

> **Важно:** Не коммитьте файл `.env` в публичные репозитории! Он уже добавлен в `.gitignore`.

Для работы GigaChat также убедитесь, что сертификат Минцифры (`rus.crt`) находится в папке `certs/`.

### 4. Запуск сервера

```bash
python app.py

```

Backend будет доступен по адресу: `http://127.0.0.1:8000`

---

## 📁 Структура проекта

* `app.py` — основной файл FastAPI, обработка изображений и логика API.
* `giga_api.py` — модуль интеграции с GigaChat. Реализована отказоустойчивость: если API недоступен, приложение продолжит работу без вывода рекомендаций.
* `models/best.pt` — обученная модель YOLOv11.
* `script.js` — логика фронтенда: загрузка фото, динамическое отображение результатов и эффект "печатной машинки" для текста.

---

## ⚙️ Особенности реализации

* **Устойчивость к ошибкам:** Если API ключ GigaChat отсутствует, запросы перестают отправляться, не нарушая работу детекции.
* **Порог уверенности:** В интерфейсе предусмотрен слайдер для настройки порога уверенности модели (Confidence Score).
* **Безопасность данных:** Реализована очистка истории Git от чувствительных данных (ключей API) с использованием `git-filter-repo`.
